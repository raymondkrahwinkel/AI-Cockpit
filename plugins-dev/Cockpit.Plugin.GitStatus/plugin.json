{
  "id": "git-status",
  "name": "Git status",
  "version": "1.3.0",
  "entryAssembly": "Cockpit.Plugin.GitStatus.dll",
  "entryType": "Cockpit.Plugin.GitStatus.GitStatusPlugin",
  "abstractionsVersion": 1,
  "minHostVersion": "1.0.0",
  "description": "A git indicator in every session's header \u2014 a coloured dot and the branch of the repo that session works in, with the uncommitted and unpushed counts on hover, refreshing when the session runs a git command. Click it to drop the summary into that session. Plus a sortable dialog for the same across all your configured repositories; manage the repository list in settings.",
  "author": "Cockpit"
}
