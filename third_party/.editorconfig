# Vendored third-party source builds on its own terms. root = true stops the repo-root .editorconfig
# cascade (which sets IDE0005 = error, tuned for the app's own code) from reaching foreign code.
root = true
