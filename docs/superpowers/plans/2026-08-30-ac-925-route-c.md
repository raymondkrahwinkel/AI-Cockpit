# AC-925 Route C Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make Cockpit's short-term Windows elevation route an explicitly separate, operator-started administrator terminal window, with its limits visible and documented.

**Architecture:** The existing `ShellExecuteEx`/`runas` route stays outside ConPTY. Its launcher becomes an internal App implementation, so no Core abstraction, session provider, TTY request, profile option, or MCP tool can request it. The menu and documentation state that Cockpit cannot observe or manage the elevated window.

**Tech Stack:** C#/.NET 10, Avalonia, xUnit.

**Spec:** AC-925 and AC-966 YouTrack comments, read 2026-08-30.

## Global Constraints

- Route C only; do not implement sudo broker or elevated PTY host.
- Keep agent sessions, profile settings, TTY launch requests, and MCP APIs free of an elevation option.
- Do not attempt to fix elevated-process Job Object containment in this ticket.
- No new dependencies.

---

## Tasks

- [x] Move the launcher from the public Core surface to an internal App helper and adjust the existing unit tests. Start with a failing test against the new location, then implement the smallest move.
- [x] Change the Windows-only menu copy so the separate administrator window and lack of Cockpit transcript, status, session-list entry, and close control are explicit.
- [x] Add focused operator documentation: route, cancellation, security boundary, and measured Job Object limitation.
- [ ] Run targeted tests, visual/headless verification if a relevant scene exists, required guard scripts, release build, rebase, quality review, and PR creation without merging.
