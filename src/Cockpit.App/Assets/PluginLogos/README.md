Eleven bundled plugins, eight local marks here — the three AI-provider plugins (Claude, Codex, Kimi) are
deliberately not among these files. Their `store.json`'s `logoAsset` names the vendor's own CDN instead
(`PluginManagerViewModel._LoadPluginLogoAsync` fetches it live, never redistributed); see the top-level
`NOTICE` file, "Provider plugin logos", for the licence/trademark decision and sources.
