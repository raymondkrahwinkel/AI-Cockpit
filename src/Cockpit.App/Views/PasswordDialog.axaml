<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Cockpit.App.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="Cockpit.App.Views.PasswordDialog"
        x:DataType="vm:PasswordDialogViewModel"
        Title="Password"
        Width="460" SizeToContent="Height" CanResize="False"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource CockpitPanelBgBrush}">

    <Design.DataContext>
        <vm:PasswordDialogViewModel/>
    </Design.DataContext>

    <DockPanel>
        <Border DockPanel.Dock="Bottom" Background="{StaticResource CockpitSecondaryBgBrush}"
                BorderBrush="{StaticResource CockpitHairlineBrush}" BorderThickness="0,1,0,0" Padding="14,12">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="OnCancel"/>
                <Button Content="Continue" Classes="Accent" IsEnabled="{Binding CanConfirm}" Click="OnConfirm"/>
            </StackPanel>
        </Border>

        <StackPanel Margin="20,18" Spacing="10">
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="12.5"
                       Foreground="{StaticResource CockpitTextSecondaryBrush}"/>

            <StackPanel Spacing="6" IsVisible="{Binding RequiresCurrent}">
                <TextBlock Text="Current password" FontSize="12"/>
                <TextBox PasswordChar="•" Text="{Binding CurrentPassword}"/>
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="Password" FontSize="12"/>
                <TextBox Name="NewPasswordBox" PasswordChar="•" Text="{Binding NewPassword}"/>
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="Password again" FontSize="12"/>
                <TextBox PasswordChar="•" Text="{Binding Confirmation}"/>
            </StackPanel>

            <TextBlock Text="Those two are not the same." IsVisible="{Binding Mismatch}"
                       FontSize="12" Foreground="{StaticResource CockpitStatusErrorBrush}"/>

            <Border Background="{StaticResource CockpitSecondaryBgBrush}"
                    BorderBrush="{StaticResource CockpitStatusWaitingBrush}" BorderThickness="0,0,0,2"
                    CornerRadius="4" Padding="10,8">
                <TextBlock Text="There is no recovery. This password is not stored anywhere — that is what makes the encryption worth having, and it means nobody can get your tokens back for you if it is lost."
                           FontSize="11" TextWrapping="Wrap"
                           Foreground="{StaticResource CockpitTextSecondaryBrush}"/>
            </Border>
        </StackPanel>
    </DockPanel>
</Window>
